#include<bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
	cin.tie(NULL);
    int x;
    int y;
    cin >> x >> y;
    cout << "Value of x: " << x << " and y: " <<y;
    return 0;
}